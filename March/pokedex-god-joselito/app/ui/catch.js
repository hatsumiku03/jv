import { useReducer } from 'react';


const Catch = ({ pokemon }) => {
    return (
      <div>
        <button className="cursor-pointer hover:bg-gray-400">Atrapar</button>
      </div>
    );
}

export default Catch;